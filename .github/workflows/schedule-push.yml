name: 📚 Daily Schedule Push

on:
  schedule:
    # 每天早上7:30 (UTC时间23:30，北京时间7:30)
    - cron: '30 23 * * *'
  workflow_dispatch: # 允许手动触发

jobs:
  push-schedule:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip [cite: 2]
        pip install requests [cite: 2]
        
    - name: 🕒 Set timezone
      run: |
        sudo timedatectl set-timezone Asia/Shanghai [cite: 4]
        
    - name: 📚 Push daily schedule with weather
      env:
        PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
        WEATHER_API_KEY: ${{ secrets.WEATHER_API_KEY }}
      run: |
        python pushplus_schedule.py [cite: 5]
        
    - name: 📝 Log execution
      if: always()
      run: |
        echo "✅ Workflow executed at $(date)" [cite: 6]
        echo "📅 Beijing time: $(TZ='Asia/Shanghai' date)" [cite: 6]
